const ROOT_PATH = __dirname.split("src")[0] + "src";
const CHAT_FILE_DIR_PATH = `${ROOT_PATH}/data/chat`;
const CHAT_FILE_PATH = `${ROOT_PATH}/data/chat/chat-messages.csv`;

export { ROOT_PATH, CHAT_FILE_DIR_PATH, CHAT_FILE_PATH };
